# Echo Voice Loop 依赖
# 安装: pip install -r requirements.txt

# 核心语音链路
openai-whisper>=20240930
edge-tts>=6.1.0
sounddevice>=0.5.0
numpy>=1.24.0
requests>=2.31.0

# 记忆系统
# SQLite3 内置，无需额外安装

# Web 服务 (Phase 4)
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# 韵律分析 (Phase 4) — 可选，无 librosa 时回退到 numpy
librosa>=0.10.0

# 唤醒词检测 (Phase 4) — 可选
# openwakeword>=0.6.0

# 树莓派额外系统依赖:
# sudo apt install mpg123 portaudio19-dev
#
# macOS 额外系统依赖:
# brew install portaudio
